---
title: 1.2 版本发布
description: Stripe、验证码、API 密钥、团队、Init CLI 等功能。
date: 2025-03-01
---

# Better Auth 1.2 – Stripe、验证码、API 密钥、团队、Init CLI 等功能

升级请运行：

```package-install
npm install better-auth@1.2
```

***

### **Stripe 插件（测试版）**

集成 Stripe 用于客户管理、订阅和 Webhook。

```package-install
npm install @better-auth/stripe
```

```ts title="auth.ts"
import { stripe } from "@better-auth/stripe"; // [!code highlight]

export const auth = betterAuth({
  plugins: [
    stripe({
      // [!code highlight]
      createCustomerOnSignup: true, // [!code highlight]
      subscription: { // [!code highlight]
        enabled: true, // [!code highlight]
        plans: [// [!code highlight]
          { // [!code highlight]
            name: "pro", // [!code highlight]
            priceId: "price_1234567890", // [!code highlight]
          }, // [!code highlight]
        ], // [!code highlight]
      }, // [!code highlight]
    }), // [!code highlight]
  ],
});
```

更多信息请参阅 [Stripe 插件文档](/docs/plugins/stripe)。

### **验证码插件**

通过 Google reCAPTCHA 和 Cloudflare Turnstile 保护你的认证流程。支持注册、登录和密码重置。

```ts title="auth.ts"
import { captcha } from "better-auth/plugins";

const auth = betterAuth({
  plugins: [
    // [!code highlight]
    captcha({
      // [!code highlight]
      provider: "cloudflare-turnstile", // 或 "google-recaptcha" // [!code highlight]
      secretKey: process.env.TURNSTILE_SECRET_KEY!, // [!code highlight]
    }), // [!code highlight]
  ], // [!code highlight]
});
```

更多信息请参阅 [验证码插件文档](/docs/plugins/captcha)。

### **API 密钥插件**

生成和管理带有速率限制、过期和元数据的 API 密钥。支持通过 API 密钥创建会话。

```ts title="auth.ts"
import { apiKey } from "better-auth/plugins";

const auth = betterAuth({
  plugins: [apiKey()],
});
```

更多信息请参阅 [API 密钥插件文档](/docs/plugins/api-key)。

### **团队/子组织**

组织现在可以拥有团队或子组织。

```ts title="auth.ts"
const auth = betterAuth({
  plugins: [
    organization({
      teams: {
        enabled: true,
      },
    }),
  ],
});
```

更多信息请参阅 [组织插件文档](/docs/plugins/organization#teams)。

### **Init CLI**

CLI 现包含 `init` 命令，可将 Better Auth 添加到你的项目中。

```bash title="terminal"
npx auth init
```

### **用户名**

* Added `displayName` for case-insensitive lookups while preserving original formatting.
* Built-in validation.

<Callout type="info">
  如果你正在使用用户名插件，请确保在你的 schema 中添加 `displayName` 字段。
</Callout>

### **组织**

* **Multiple Roles per User** – Assign more than one role to a user.

### **管理员插件**

* Manage roles and permissions within the admin plugin. [Learn more](/docs/plugins/admin)
* `adminUserIds` option to grant specific users admin privileges. [Learn more](/docs/plugins/admin#usage)

***

## 🎭 新增社交登录提供商

* [TikTok](/docs/authentication/tiktok)
* [Roblox](/docs/authentication/roblox)
* [VK](/docs/authentication/vk)

***

## ✨ 核心增强功能

* **Auto Cleanup** for expired verification data
* **Improved Google One Tap** integration with JWT verification and enhanced prompt handling
* **Phone-based Password Reset** functionality
* **Provider Control Options**:
  * Disable signups for specific providers
  * Disable implicit signups for specific providers
  * Control default scopes and allow custom scopes on request
* **Enhanced Database Hooks** with additional context information

***

## 🚀 性能提升

我们重写了 **better-call**（Better Auth 的核心库）以解决 TypeScript 编辑器卡顿问题。现在你的 IDE 使用 Better Auth 时会更加流畅。

***

## ⚡ CLI 功能增强

### **`init` 命令**

CLI 现包含 `init` 命令，加快初始化速度：

* Scaffold new projects
* Generate schemas
* Run migrations

[了解更多](/docs/concepts/cli)

***

## 🛠  bug 修复与稳定性改进

大量修复与优化，让功能更流畅、更快速、更稳定。详情请查看[更新日志](https://github.com/better-auth/better-auth/releases/tag/v1.2.0)。

***

```package-install
npm install better-auth@1.2.0
```

**马上升级，体验这些强大新功能！** 🚀
