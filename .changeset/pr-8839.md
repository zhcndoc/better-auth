---
"better-auth": 补丁
---

当 `emailAndPassword.autoSignIn` 为 `false` 时应用邮箱枚举保护。重复注册现在会返回一个合成用户（`token: null`）并触发 `onExistingUserSignUp`，新的注册也会跳过自动登录（`token: null`）——即使没有 `requireEmailVerification`，也与文档保持一致。
